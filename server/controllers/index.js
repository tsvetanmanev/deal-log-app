const home = require('./home-controller')
const users = require('./users-controller')
const records = require('./records-controller')

module.exports = {
  home: home,
  users: users,
  records: records
}
